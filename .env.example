# n8n Configuration
# Your domain name for n8n (without https://)
N8N_DOMAIN=n8n.yourdomain.com

# n8n Basic Authentication
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your-secure-password-here

# PostgreSQL Database Configuration
POSTGRES_DB=n8n
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=your-database-password-here

# Let's Encrypt SSL Certificate
# Email address for Let's Encrypt certificate registration
ACME_EMAIL=your-email@example.com

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual configuration
# 3. Use strong passwords for POSTGRES_PASSWORD and N8N_BASIC_AUTH_PASSWORD
# 4. Ensure N8N_DOMAIN points to your server's IP address
# 5. Use a valid email for ACME_EMAIL for Let's Encrypt notifications